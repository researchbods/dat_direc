language: ruby
cache: bundler
rvm:
  - 2.3
  - 2.5
  - 2.6

before_install:
  - yes | gem update --system --force
  - gem install bundler
script:
  - gem build
  - bundle exec rspec --format progress
